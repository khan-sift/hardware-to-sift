# Pinned to the exact version every lab was built and validated against.
# Newer 0.x builds drift in ways that break the labs (e.g. StreamingMode is no
# longer re-exported from sift_client.resources), and v1.0 removes the
# deprecated sift_py module. See docs/hardware-to-sift-framework.md, Section 5.
sift-stack-py==0.17.0
pymavlink
